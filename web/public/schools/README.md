# Schools landing images

Drop the GPT-generated illustrations for the **schools** landing page here,
as compressed `.webp` (same paper-cutout teal style as `/public/fam`).

Expected filenames (wired, or to be wired, in `SchoolsLandingClient.tsx`):

Problem section (the 3 callouts):
- `gap.webp`      — "the compounding gap" (פער שמתעצם)
- `time.webp`     — "the time drain" (זמן שהולך לאיבוד)
- `silent.webp`   — "the silent dropout" (הניתוק השקט)

Feature / how-it-works illustrations (one per part):
- `projector.webp`   — teacher explains a word on the classroom projector
- `own-language.webp` — a student reads the word in their own language
- `picture.webp`     — a word shown with its picture
- `roster.webp`      — teacher dashboard / class roster (no per-student login)

Keep each image ~1200px wide, WebP, well under 200 KB. Alt text stays empty
(decorative); the copy carries the meaning.
